export default defineAppConfig({
  ui: {
    primary: "blue",
    gray: "cool",
    icons: {
      dynamic: true, // use icones directly from icones.js.org
    },
    button: {
      size: "lg",
    },
  },
});
